{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "properties": {
        "value": {
            "type": "string",
            "minLength": 1,
            "maxLength": 1000
        },
        "format": {
            "type": "string",
            "minLength": 1,
            "enum": ["html", "text"]
        }
    },
    "required": [
        "value",
        "format"
    ]
}
